name: "CD: Main Workflow"
run-name: ${{ github.actor }} Deployment Workflow

env:
  VERCEL_TOKEN: ${{ secrets.FIRST-NEXTJS-PROJECT.VERCEL_TOKEN }}
  VERCEL_ORG_ID: ${{ secrets.FIRST-NEXTJS-PROJECT.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.FIRST-NEXTJS-PROJECT.VERCEL_PROJECT_ID }}

on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Installation
        run: yarn install

      - name: Run Deploy
        run: yarn deploy
